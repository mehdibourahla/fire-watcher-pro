node_modules
.output
.nitro
.wrangler
.vercel
dist
.DS_Store

# secrets — never commit credentials
.env
.env.*
!.env.example
*.pem

# agent worktrees are separate git repos, never part of this one
.claude/worktrees/
