# antislop audit 001, 2026-09-25

Scope: shipped revamps at `b797fa5`: zones (#161, #162), fire detail (#163), admin console (#160).
Method: code inspection; contrast computed from `src/styles.css` tokens. Every item re-verified by the lead.
Priority follows the rule tier: Hard Gate HIGH, Purpose-Gate MEDIUM, Quality Locks LOW.

## HIGH

1. R-27 fire: `FireDetailPage.tsx:84` treats a failed query as "fire not found"; `isError` is never read, so a
   network error has no retry.
2. R-32 fire: `Explain.tsx:22` + `StatCard.tsx:26`: the tooltip trigger is a plain `div` with no `tabIndex`;
   keyboard users never reach any explainer.
3. R-25 zones: dark theme `--emergency #ee3b3b` on `--emergency-surface #341e20` is 3.94:1 for small text
   (default-point warning `ZoneCard.tsx:81-88`, shared `ErrorState` `states.tsx:72-80`).
4. R-25 admin: `AdminNav.tsx:59` 12 px group labels in `text-faint`: 3.08:1 on `--ground`, 3.21:1 on `--surface`.
5. R-03 all: `ui/button.tsx:24-27` heights 32 to 36 px, no coarse-pointer override; `ErrorState` retry about
   26 px. Zones 5 small buttons, admin 25.
6. R-27 zones: raw backend `error.message` shown untranslated (`ZonesPage.tsx:90`, `ZoneCard.tsx:54,160`,
   `ZoneEditor.tsx:164,343`); only the list error offers a next action.
7. R-02 zones: em dash in `account.zoneFires`, `account.zoneFollowsNothing`.
8. R-02 fire: em dash in `fire.metaTitle`, `fire.windToward`, `fire.growthGrowing`, `fire.growthFading`,
   `explain.area`, `evidence.official`, `risk.fuelLimited`.
9. R-02 admin: em dash in `broadcastAdmin.killOn`, `killOff`, `relayNote`, `queues.applyNote`, `people.noZones`,
   `sources.pushTestWaiting`; hard-coded in `BroadcastConsole.tsx:207` (also untranslated), `ita/AreaPicker.tsx:57`,
   `OfficialIncidents.tsx:38`.
10. R-02 + R-27 admin: 13 lone "—" as empty-value markers that give no reason (`MemberDetail.tsx:29`,
    `SourceArchive.tsx:84-85`, `BroadcastConsole.tsx:242,392`, `kit/When.tsx:9`, `FireDetail.tsx:80,84,89,92`,
    `PeopleDirectory.tsx:419`, `OfficialIncidents.tsx:40`, `AuditLog.tsx:25`).

## MEDIUM

11. R-06 fire + admin: uppercase wide-tracked labels with no written reason (`StatCard.tsx:37`,
    `IdeaQueue.tsx:75`, `AdminNav.tsx:59`).
12. R-04 all: `lucide-react` is the only icon set, reason never written.

## LOW

13. C-3 zones: `ZoneCard.tsx:78` shows the zone's alert threshold as an unlabelled `RiskChip`; it reads as the
    zone's current danger.
14. Multi-hazard framing (project rule, not antislop): `account.zoneFires` says "active fires in this zone".

Clean: shadow and glow doses, no endless animation, no generic icons or CTAs, no dead links, admin empty states
all specific, admin home is an attention list rather than a stat-card dashboard.

## Outcome (branch `antislop-fixes`)

1. Fixed: a failed fire query shows an error with retry (`fire.loadError`).
2. Fixed: explainer triggers take `tabIndex={0}`; focus opens the tooltip (verified in browser).
3. Fixed: new `--emergency-ink` token for text on `--emergency-surface` (dark 9.43:1) at 15 sites; dark `--emergency`
   nudged to `#f04444` so small red text and destructive buttons reach 4.72:1 on `--surface`.
4. Fixed: admin nav labels use `text-muted-foreground` (7.12:1 dark, 5.98:1 light).
5. Fixed: buttons and the error retry reach 44 px on coarse pointers (`pointer-coarse:`); mouse layouts unchanged.
6. Fixed: zone errors are translated; the zone-limit trigger message maps to `account.zoneLimit`, pinned by
   pgTAP and a unit test.
7-9. Fixed in all locales; every `metaTitle`, `defaultTitle`, the title template and the theme labels use `·`
   or a full stop. Two dashes written as unicode escapes in `explain.area` were missed by the first grep and are fixed.
10. Fixed: empty values say Not set, Unknown, No role, None, Not recorded or Choose a wilaya.
11. Fixed: no uppercase wide-tracked labels on the three surfaces or the forecast legend.
12. Kept `lucide-react`; reason written in the 2026-08-28 design spec.
13. Fixed: the zone card labels its threshold chip with "Warn me from danger level".
14. Subtitle reworded to hazards. The per-hazard live status on each zone card is a feature, queued separately.

Left for their own page revamps (per-page rule): em dashes on /developers, /survival and other unaudited pages.
