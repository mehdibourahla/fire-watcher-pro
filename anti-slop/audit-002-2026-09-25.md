# antislop audit 002, 2026-09-25

Scope: surfaces shipped since audit 001 (`b797fa5`) without an audit, at `17ac018`: the report flow (#168),
the map's new detail views (#170 to #174: citizen reports, earthquakes, airport weather, official detail),
and the admin additions (report moderation, the zone push table, the push test of #177). Forecast and
history had their own gate in #166.
Method: code inspection; a sweep proposed candidates, every item below re-read at its line by the lead.
Priority follows the rule tier: Hard Gate HIGH, Purpose-Gate MEDIUM, Quality Locks LOW.

## HIGH

1. R-27 admin: `PushDeviceTest.tsx:12-15` never reads `arrival.isError`, and the one-minute expiry is computed
   only inside a successful poll. If the poll fails, the button says "waiting" forever.
2. R-27 admin: `PushDeviceTest.tsx:50` shows the server's English error text (`push-test.server.ts`, e.g.
   "Please wait one minute") in every locale.
3. R-27 admin: `ReportModeration.tsx:240` shows the raw PostgREST error to moderators, untranslated.
4. R-02 map: em dash in `civilMap.offline` (map list offline notice) and `official.disclaimer` (official
   incident detail), in all four locales.
5. R-02 + R-27 map: `CivilSituation.tsx:406,412` renders a lone "—" for an unknown fire area and peak FRP,
   with no reason (the pattern audit 001 item 10 removed from admin).
6. R-03 map: `HazardReportDetail.tsx:98` the "sign in to confirm" call to action is an inline text link with
   no 44 px target.
7. R-03 admin: `BroadcastConsole.tsx:202` the authority-warning form's selects are 36 px with no coarse-pointer
   override (`ui/button` has one since audit 001; this raw `select` class does not).

## MEDIUM

8. R-27 report: `report.tsx:475` a failure loading "My reports" says only "Something went wrong", with no
   retry.
9. R-06 map: `CivilSituation.tsx:156` the situation card's source label is 11 px uppercase wide-tracked with
   no written reason (audit 001 item 11 removed these elsewhere).
10. Four-locale rule (project): `BroadcastConsole.tsx:268-269` severity options "Severe" and "Extreme" are
    hard-coded English in every locale.

## LOW

11. R-27 report: `report.tsx:66` never reads `units.isError`; if places fail to load, the commune line
    silently disappears from the report form.
12. R-26 map: `HazardReportDetail.tsx:97` signing in to confirm a report returns to the map root
    (`returnTo: "/"`), so the report being confirmed is lost.
13. R-38 report: `LevelCard.tsx:85` "People alerted" counts people whose in-app alert included the report,
    not pushes delivered; the label does not say which.

14. R-20 + C-3 all (added by the owner): lists have no end. Only the admin audit log paged. The translation
    editor rendered 1,202 editable rows with every group open; the map list is one card per situation (the worst
    day sent 1,171 ONM warnings); history stopped at 300 rows with no way to reach the rest; about 15 other pages
    showed their database cap (50 to 500 rows) in one go, and the server sent all of it. Prod on 2026-09-25:
    1,502 fire clusters and 10,113 ONM warnings in 30 days, 228 clusters on the worst day.

Rejected: the sweep also flagged `admin/kit/QueryState.tsx:38` for printing the backend message. It sits under
a translated heading with a retry button, so cause and next action are given; the raw detail is for
operators. Not a defect.

Clean: Arabic strings (no leftover English beyond the proper noun "Info Trafic Algérie"), muted-text contrast
on raised surfaces (5.24:1 light, 6.29:1 dark), no endless motion or decorative dots, the report form's tiles
and buttons reach 44 px, quake and airport details use translated strings only, LevelCard points match the
SQL formula and its explanation, report errors are translation keys.

## Outcome (branch `antislop-002-fixes`, owner picked 1 to 7)

1. Fixed: the arrival wait is measured from the send time, so polling stops after a minute even when every
   check fails; a failed check then says so (`sources.pushTestCheckFailed`) instead of "waiting".
2. Fixed: push-test failures carry translation keys (`PushTestError`, `pushTestErrorKey` maps 401, 403, 429,
   400/413 and the rest); verified in the browser with notifications off.
3. Fixed: moderation errors map `report_moderator_role_required` and `report_not_found` to their own messages,
   anything else to "The decision was not saved. Try again." Kabyle admin falls back to English as before.
4. Fixed in all four locales: both strings are split into sentences.
5. Fixed: unknown area and peak FRP read "Not recorded" (`common.notRecorded`).
6. Fixed: the sign-in-to-confirm link is a 44 px target.
7. Fixed: the authority-warning selects reach 44 px on coarse pointers (`pointer-coarse:h-11`, as `ui/button`).

## Outcome (branch `audit-002-rest`, owner picked the rest)

8. Fixed: "My reports" failure names the cause and offers Try again (`reports.mineError`).
9. Fixed: the situation card source label is 12 px sentence case, no tracking.
10. Fixed: CAP severity, certainty and urgency are translated (`cap.*`, the five CAP 1.2 values each) in the
    admin warning form and in the ONM detail on the map, which also showed them raw; an out-of-standard value
    falls back to itself.
11. Fixed: if place names fail to load, the report form says so and that the exact position is still recorded.
12. Fixed, and wider than audited: `authDestination` never allowed `/`, so signing in to confirm landed on
    `/zones`. The map root with its query is now an allowed destination (unit-tested; `//host` still rejected)
    and the link returns to the report being confirmed.
13. Fixed: the stat reads "People alerted in the app".

## Outcome of item 14, part 1 (branch `show-more`)

- The server sends 25 rows at a time (`src/lib/paging.ts`: offset pages, a row pushed across a page boundary is
  shown once) and a "Show more" button fetches the next page. Moved to this: alerts (unread count and "mark all
  read" are now server-side, so they no longer depend on what is loaded), My reports, published ideas, webhook
  deliveries, admin fires, source incidents and gaps, the source archive, the ITA feed and queue, and official
  incidents (the listed/unlisted filter now runs on the server). Admin `QueryState` adds the button for any
  paged query; the audit log's own button is gone.
- The map list keeps its data (every item is also a map marker) and shows 20 per group with "Show N more".
- The translation editor opens with every group collapsed; a search opens the matching groups. Its tab title
  also showed "{{language}}" raw; fixed.

Part 2 (people, translation moderation, idea moderation, report moderation: their database functions need page
and filter parameters) and part 3 (history: records paged on the server, charts from a summary query) follow.
